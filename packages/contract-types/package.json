{
  "name": "daimo-contract-types",
  "version": "1.0.0",
  "description": "Typescript types and ABIs for daimo contracts",
  "types": "./dist/contract-types/src/index.d.ts",
  "main": "./dist/contract-types/src/index.js",
  "scripts": {
    "generate": "wagmi generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run generate && npx tsc"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@tsconfig/node20": "^1.0.1",
    "@wagmi/cli": "^1.1.0",
    "typescript": "^5.1.3"
  }
}